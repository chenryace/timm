# Optional: Prefix for all store paths.
# For PostgreSQL, this would typically be a schema name or part of a table naming convention if used.
# Example: "myapp/notes" or "production"
STORE_PREFIX=""

# PostgreSQL connection URL (required)
# Format: postgresql://username:password@host:port/database_name
# Example: postgresql://user:secret@localhost:5432/notea_db
POSTGRES_URL=""

# Password for the application (used for session encryption, etc.)
# Ensure this is a strong, unique password in production.
PASSWORD=123
